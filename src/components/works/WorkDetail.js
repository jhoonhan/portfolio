import React, { useRef } from "react";

const WorkDetail = () => {
  const render = () => {
    return (
      <div className="work__detail-container">
        <p>aaang</p>
      </div>
    );
  };

  return render();
};

export default WorkDetail;
